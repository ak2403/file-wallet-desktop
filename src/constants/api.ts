const UserBaseUrl = '/user';
const DeviceBaseUrl = '/device';

export const UserSpecificUrl = {
  registerUser: `${UserBaseUrl}/create`,
};

export const DeviceSpecificUrl = {
  registerDevice: `${DeviceBaseUrl}/create`,
};

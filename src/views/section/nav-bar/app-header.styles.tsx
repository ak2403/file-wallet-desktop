import styled from 'styled-components';

import { Colors } from '../../../config/colors';

export const ConnectionHeader = styled.h1`
  margin-top: 30px;
  font-family: 'Lato', sans-serif;
  color: ${Colors.appWhite};
`;
